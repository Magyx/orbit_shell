[package]
name = "lockscreen"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
orbit-api = { path = "../../src/orbit-api" }
orbit-keys = { path = "../../module-libs/keys" }

duration-str = "0.21.0"
pam = "0.8.0"
libc = "0.2.185"
